# FastAPI and Web Server
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Sui Integration (commented out for Vercel - can be large)
# pysui==0.65.0
# For local dev with Sui integration, uncomment above line

# Walrus and HTTP
httpx>=0.27.0,<0.28

# LangChain and RAG (commented out for Vercel - too large)
# langchain==0.1.0
# langchain-openai==0.0.2
# langchain-community==0.0.10
# For local dev with RAG, uncomment above lines

# Vector Database
# chromadb==0.4.22  # Commented out for Vercel deployment (too large)
# Use ChromaDB Cloud or external service instead
# For local dev, uncomment above line

# OpenAI (commented out - only needed for RAG)
# openai==1.6.1
# For local dev with RAG, uncomment above line

# PDF Processing (commented out - only needed for RAG)
# PyPDF2==3.0.1
# For local dev with RAG, uncomment above line

# Configuration
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Utilities
# tiktoken==0.5.2  # Only needed for langchain tokenization
# For local dev with RAG, uncomment above line
